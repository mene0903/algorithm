배열의 특징
index를 사용하여 값에 바로 접근 가능
새로운 값을 삽입하거나 특정 인덱스에 있는 값을 삭제하기 어려움. 삽입/삭제 시 해당 인덱스 주변에 있는 값 이동시켜야 함
크기는 선언할 때 지정할 수 있으며, 크기 늘이거나 줄이기 불가능
구조 간단

리스트 특징
노드를 포인터로 연결한 자료구조
인덱스가 없으므로 값에 접근하려면 Head 포인터부터 순차적 접근 (접근 속도 느림)
포인터 연결되어 있으므로 삽입 삭제 빠름
크기 선언 안해도 됨. 크기가 변하기 쉬운 데이터가 적절
배열보다 구조 복잡

구간합
시간 복잡도가 O(N)에서 O(1)로 줄어듬
